droutes:

product:
    get => all product
    post => add new product[title, description, imageFile]

uploads:
    get/id => send product id, get product image

TODO: use passanger, use request limit,
